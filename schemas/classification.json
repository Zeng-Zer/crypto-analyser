{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CryptoEventClassification",
  "description": "LLM classification output for a crypto price anomaly event",
  "type": "object",
  "properties": {
    "classification": {
      "type": "string",
      "description": "Primary classification of the price anomaly",
      "enum": [
        "explained",
        "unexplained",
        "flash_crash",
        "coordinated_dump",
        "technical_issue",
        "news_driven",
        "derivatives_driven",
        "unknown"
      ]
    },
    "severity": {
      "type": "integer",
      "description": "Severity score on a scale of 1 (minor) to 10 (catastrophic)",
      "minimum": 1,
      "maximum": 10
    },
    "confidence": {
      "type": "number",
      "description": "Confidence in the classification, from 0.0 (uncertain) to 1.0 (certain)",
      "minimum": 0.0,
      "maximum": 1.0
    },
    "rationale": {
      "type": "string",
      "description": "Detailed reasoning for the classification, citing specific evidence from the derivatives context",
      "minLength": 10
    },
    "news_relevance": {
      "type": "string",
      "description": "Optional: estimated lag of news relevance (15min-60min if lagging, or 'concurrent' if aligned). Only populate if RSS context was provided.",
      "enum": [
        "concurrent",
        "15min_lag",
        "30min_lag",
        "60min_lag",
        "irrelevant"
      ]
    }
  },
  "required": [
    "classification",
    "severity",
    "confidence",
    "rationale"
  ],
  "additionalProperties": false
}
