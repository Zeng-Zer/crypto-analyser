# Ignore all generated data outputs (parquet, duckdb, JSON, reports, ...).
# Directory scaffolding stays via .gitkeep. Anything produced by the pipeline
# is regenerable from source data + scripts, so it never belongs in the repo.
*
!.gitignore
!.gitkeep