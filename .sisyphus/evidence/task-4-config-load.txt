Configuration system validation

Command: uv run python -c "import yaml; data = yaml.safe_load(open('config/settings.yaml')); ..."

Result: PASS - YAML loads successfully, all keys present
- api_keys, sources, anomaly_detection, rss_feeds, events, paths, pgvector, llm, langfuse, ragas
- Z-score threshold: 2.5
- Binance symbol: LUNAUSDT
- Events: luna, ftx, bybit
- RSS feeds: CoinDesk, CoinTelegraph, Decrypt

---

Placeholder detection test

Command: load_config() with unfilled keys

Result: PASS - RuntimeError raised with:
  "Placeholder values detected in config: api_keys.llm_api_key, langfuse.public_key, langfuse.secret_key"

---

Filled keys test

Command: load_config() with test API keys

Result: PASS - Config loads successfully, typed properties accessible
- config.anomaly_detection['threshold'] = 2.5
- config.sources['binance']['symbol'] = 'LUNAUSDT'
- config.langfuse['host'] = 'http://localhost:3000'
